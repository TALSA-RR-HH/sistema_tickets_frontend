export const environment = {
    production: true,

    // OPCIÓN 1: LOCAL (Para trabajar en tu PC)
    // apiUrl: 'http://localhost:8080/api'

    // OPCIÓN 2: NUBE (Para subir a Vercel/Render)
    apiUrl: 'https://sistema-tickets-backend.onrender.com/api'
};